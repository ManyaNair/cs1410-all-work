package codingBat;

public class codingBatPractice
{
	/*
	public static String wordEnds(String str, String word)
	{
		
		 * String store = "";
		int countOfMatches = 0;
		
		for (int i = 0; i < str.length(); i++)
		{
			if(str.charAt(i) == word.charAt(0) && i + word.length() <= str.length())
			{
				countOfMatches = 0;
				for (int j = 0; j < word.length(); j++)
				{
					if(str.charAt(i+j) == word.charAt(j))
					{
						countOfMatches++;
						//System.out.println(countOfMatches);
					}
				}
				if (countOfMatches == word.length()) 
				{
					if(i-1 >= 0)
					{
						store += str.charAt(i-1);
					}
					if(i + word.length() < str.length())
					{
						store += str.charAt(i + word.length());
					}
				}
			}
		}
		return store;

	}

		 
		
	}
	*/
}
